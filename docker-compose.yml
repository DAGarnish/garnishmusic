services:
  postgres:
    image: postgres:18
    restart: unless-stopped
    environment:
      POSTGRES_DB: garnishmusic
      POSTGRES_USER: garnishmusic
      POSTGRES_PASSWORD: garnishmusic
    volumes:
      - postgres_data:/var/lib/postgresql
    ports:
      - "5432:5432"

volumes:
  postgres_data:
